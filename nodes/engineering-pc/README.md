# Engineering PC Node

The engineering PC is the preferred home for media-heavy and hardware-accelerated
work when a suitable local GPU is available.

Recommended roles:

- `core`
- `media-gpu`
- optional `tools`

This node should specialize in model-heavy generation, workflow experimentation,
and GPU-bound execution. It should not become an ad hoc second hub.
